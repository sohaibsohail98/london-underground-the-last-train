#pragma once

// Automation test coverage for train boarding (LastTrain.Boarding.TrainInteractableAndBoard).
// The test itself self-registers via IMPLEMENT_SIMPLE_AUTOMATION_TEST in
// LTBoardingTest.cpp; this header exists only so that .cpp has its own header to
// include first, per convention.
