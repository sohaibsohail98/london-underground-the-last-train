# Remote brief: pause menu Source hook

This file is the handoff prompt for a remote Claude session working this
branch, verbatim. Follow it as the task brief.

---

Repo: london-underground-the-last-train, branch off main. This is LAST TRAIN, a first person zombie survival game on a fictionalised London Underground line, built in Unreal Engine 5.8. Read CLAUDE.md at the repo root first, in full, for every project convention: British spelling everywhere including comments and docs, never use em or en dashes anywhere (source, docs, this branch's own commit messages), TObjectPtr not raw UObject* in containers, LT_LOG not UE_LOG, no TODO/FIXME/HACK markers, clang-format 20 with tab indent, headers begin with #pragma once and end their include list with X.generated.h. You have no access to the Unreal Editor and cannot compile or run PIE: write C++ against the existing module conventions, do not attempt to build or test it in-engine, and say so plainly in your summary rather than claiming it compiles.

Task: add the Source/ hook a pause menu needs. There is currently no pause system at all: no PauseAction UInputAction, no UGameplayStatics::SetGamePaused call, nothing. Read docs/tasks/handover.md S13's row in full first (search for "S13 Main menu") - it documents the main menu Blueprint (BP_MenuGameMode, WBP_MainMenu) already built and explicitly flags, as a noted follow-up, that there is no return to the main menu from the death or pause flow yet and that this needs "a pause menu" wired in later. This branch is that later: the C++ half only, not the widget.

Read Source/LastTrain/Public/Player/LTPlayerCharacter.h and its .cpp in full first, to learn the existing Enhanced Input convention exactly (see MoveAction, LookAction, JumpAction, SprintAction, FireAction, AimAction, ReloadAction, InteractAction: all EditDefaultsOnly TObjectPtr<UInputAction>, null until a Blueprint assigns them, bound in SetupPlayerInputComponent). Also read Source/LastTrain/Public/Core/LTGameState.h and LTGameMode.h/.cpp for ELTRunState and how the game mode currently flips it, since pause needs to interact with run state sensibly (pausing during PreGame, Active or Downed should all work; pausing after Dead or during Boarded travel does not make sense and should be refused, not silently allowed).

Scope:

1. Add a PauseAction UInputAction property to ALTPlayerCharacter, following the exact existing pattern (EditDefaultsOnly, null default).
2. Add a UFUNCTION(BlueprintCallable) TogglePause() (or separate RequestPause()/RequestResume() if that reads cleaner against the existing StartAim()/StopAim() style pairing already used elsewhere in this class - match whichever pairing convention is already dominant in the file) that:
   - Calls UGameplayStatics::SetGamePaused on the world.
   - Sets the player controller's input mode and mouse cursor visibility appropriately (UI-focused with cursor shown when paused, game-focused with cursor hidden when resumed), following the same Set Input Mode / Set Show Mouse Cursor pattern S13's BP_MenuGameMode already uses (read that row in handover.md again for the exact call sequence it used, so this matches rather than reinvents it).
   - Refuses to pause when ELTRunState is Dead or Boarded (check ALTGameState's current run state before pausing), so the pause action is a no-op rather than a confusing frozen screen during a state transition that already has its own UI (death/boarded already have or will have their own screens per G2's spec, they do not also need a pause overlay on top).
   - Fires a UPROPERTY(BlueprintAssignable) delegate (FOnPauseStateChanged or similar, follow the existing FOnRunStateChanged-style naming already used in this codebase, e.g. LTGameState.h) broadcasting the new paused/unpaused state, so a Blueprint widget can show/hide itself off this rather than polling.
3. Bind PauseAction in SetupPlayerInputComponent, same block style as the other bindings.
4. Do not build the WBP_PauseMenu widget itself, do not touch Content/, that is editor work for CC-in-Unreal. This branch is the Source/ hook only, matching the project's own division of labour (see docs/tasks/who-does-what.md if useful context: CC-in-Unreal owns all Blueprint/widget/editor work, this branch stays in Source/).

5. Write a short new task spec, docs/tasks/phase-g4-pause-menu.md, modelled on the structure of an existing short Phase task file (phase-g2-hud.md is a good length reference, though this one is much smaller in scope). It should tell CC-in-Unreal: bind PauseAction in the Enhanced Input asset (default a reasonable key, e.g. Escape, without touching any Source/ default since the property is intentionally null until Blueprint-assigned, same as every other input action in this project), build WBP_PauseMenu with resume/settings/quit-to-main-menu options in the project's established HUD visual language (read docs/reference/reference-frame-notes.md section 3 and whatever G2 HUD styling has landed by the time this is picked up), bind it to the new OnPauseStateChanged delegate, and route "quit to main menu" through Open Level to L_MainMenu (the same pattern WBP_MainMenu's own Quit button already uses in reverse, per S13). Flag as an open question whether pause should be reachable while Downed (the bleed-out clock arguably should not stop just because the player opened a menu, or arguably should, since pausing an active bleed-out timer could be exploited to stall forever - state the tension plainly, recommend an answer, but leave the final call to CC-in-Unreal or the project owner rather than deciding unilaterally in this Source/-only branch).

Rules: British spelling, no dashes, TObjectPtr not raw pointers, LT_LOG not UE_LOG, no TODO markers, GENERATED_BODY present, headers end their includes with X.generated.h. Keep commits small: the input action and pause function as one commit, the spec doc as a separate commit, matching the zombie surface masks branch's discipline (tools/feature commit, then docs commit).

When done: do not merge or push anything, just leave the branch in a clean, reviewable state. Report back which files you touched, the exact new API surface added (property names, function signatures, delegate name), and explicitly flag the Downed-state pause question above rather than silently picking an answer.
