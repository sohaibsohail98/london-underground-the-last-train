# Remote brief: save and settings system

This file is the handoff prompt for a remote Claude session working this
branch, verbatim. Follow it as the task brief.

---

Repo: london-underground-the-last-train, branch off main. This is LAST TRAIN, a first person zombie survival game on a fictionalised London Underground line, built in Unreal Engine 5.8. Read CLAUDE.md at the repo root first, in full, for every project convention: British spelling everywhere including comments and docs, never use em or en dashes anywhere (source, docs, this branch's own commit messages), TObjectPtr not raw UObject* in containers, LT_LOG not UE_LOG, no TODO/FIXME/HACK markers, clang-format 20 with tab indent, headers begin with #pragma once and end their include list with X.generated.h. You have no access to the Unreal Editor and cannot compile or run PIE: write C++ against the existing module conventions, do not attempt to build or test it in-engine, and say so plainly in your summary rather than claiming it compiles.

Task: build the disk save system the codebase already names as owed work. Read Source/LastTrain/Public/Core/LTGameInstance.h in full first: its own doc comment on FLTTravelPayload states plainly "Held in memory on the game instance for the session only: a disk save game is a later task." Nothing implements that later task yet: no USaveGame subclass exists anywhere in Source/LastTrain. This branch is that task.

Read docs/design/gameplay-canon.md and docs/tasks/phase-g-audio-hud-balance.md's G3 section (balance) for anything already stated about progression, best-round tracking, or persistence across runs, so the save data schema you design actually matches what the game currently tracks rather than inventing new mechanics. Read Source/LastTrain/Public/Economy/LTPointsComponent.h and Source/LastTrain/Public/Rounds/LTRoundManager.h for what a "run" currently consists of (points, round reached), and Source/LastTrain/Public/Player/LTPlayerCharacter.h for the Enhanced Input action properties (the settings half of this task needs to persist key bindings against these, if Enhanced Input's own remapping system is used - check whether UEnhancedInputUserSettings or a simpler custom approach is more appropriate for this project's scope, and say which you chose and why rather than defaulting to the heaviest option).

Scope, two parts:

1. Run/progression save data. A new ULTSaveGame : public USaveGame (Source/LastTrain/Public/Core/LTSaveGame.h, matching the existing Core/ module layout alongside LTGameInstance, LTGameMode, LTGameState). Track, per station map name (FName, matching the pattern ULTGameInstance's VisitedStations already uses): best round reached, total runs started, total runs survived to boarding at least once. Keep the schema small and directly justified by what the game already measures, do not invent scoring systems, achievements or unlocks that are not asked for anywhere in the design docs.

2. Settings save data, in the same ULTSaveGame or a second ULTSettingsSaveGame if that separation is cleaner (say which you chose and why): master volume (a single float is enough given G1 audio's own scope is still being built out, do not assume a mix bus structure that does not exist yet), field of view or sensitivity if LTPlayerCharacter already exposes tunable properties for either (check the file rather than assuming), and whatever key binding persistence the Enhanced Input approach you chose in the paragraph above actually needs.

3. Wire load/save into ULTGameInstance: load on Init() (already exists, extend it), save at sensible points - after a run ends (Dead or a successful board, check how ALTGameMode currently detects these via ELTRunState) and whenever a settings change is applied. Follow the existing UFUNCTION(BlueprintCallable) pattern so a menu widget can call an explicit "apply and save settings" function rather than this being fully automatic and invisible, matching how every other cross-system call in this codebase is an explicit function call, not an implicit hook.

4. Write a short new task spec, docs/tasks/phase-g5-save-settings.md, telling CC-in-Unreal what Blueprint/widget work remains: a settings panel (likely reachable from both the main menu WBP_MainMenu, per S13 in docs/tasks/handover.md, and the pause menu once docs/tasks/phase-g4-pause-menu.md lands, if it has by the time this is read - check), wiring slider/dropdown widgets to the new BlueprintCallable apply-and-save functions, and displaying best-round-reached somewhere sensible (the main menu is the obvious spot, note it as a suggestion not a mandate). Keep this spec short, this is a small, bounded follow-up, not a new phase-sized document.

Rules: British spelling, no dashes, TObjectPtr not raw pointers, LT_LOG not UE_LOG, no TODO markers, GENERATED_BODY present, headers end their includes with X.generated.h. Do not touch Content/ or build any widget. Keep commits small: the save game class(es) as one commit, the game instance wiring as a second, the spec doc as a third, matching the zombie surface masks branch's discipline.

When done: do not merge or push anything, just leave the branch in a clean, reviewable state. Report back which files you touched, the exact schema of both save classes, which Enhanced Input remapping approach you chose and why, and explicitly flag anything you were not confident about (in particular: whether UEnhancedInputUserSettings is the right level of complexity for this project's current scope, or whether a simpler custom key binding struct is more appropriate) rather than guessing silently.
