Repo: london-underground-the-last-train, on main (pull first, this branch just merged: SetDecalMaterials, SpawnBloodDecal, SpawnDeathPool on ULTGoreDecalSubsystem already exist in Source/ and are compiled). This is LAST TRAIN, a first person zombie survival game on a fictionalised London Underground line, built in Unreal Engine 5.8. Read CLAUDE.md at the repo root first, in full, for every project convention before touching anything: British spelling everywhere including comments and docs, never use em or en dashes anywhere, TObjectPtr not raw UObject* in containers, LT_LOG not UE_LOG, no TODO/FIXME/HACK markers, the palette (#16161C, #6C4C9C, #E0A030, #B02030), and the legal section (no roundel, no Johnston typeface, no official line diagram, no operator livery, no Call of Duty naming). This is an editor task: you have a live MCP connection to the running Unreal Editor. Never mutate an actor, asset or level while PIE is running, stop PIE cleanly first. MAP CHECK as a bare console exec is banned, use MAP CHECKDEP NOCLEARLOG (docs/known-issues.md 2.13 and editor-crash-endplaymap.md explain why).

Task: read docs/tasks/phase-h2-gore-system.md in full, it is the complete spec and is more detailed and more authoritative than this prompt on every point of overlap. This prompt is a short pointer to it, not a replacement for it. Read docs/art-direction.md section 6 before tuning anything, it is the whole design brief for this feature in four sentences: restraint, high value moments over uniform coverage, blood concentrated near the fight rather than sprayed everywhere, decals pooled and capped with the oldest recycled.

Three steps, in order:

1. Assign the decal materials. ULTGoreDecalSubsystem is a UWorldSubsystem with no archetype to edit in the content browser, so this is a Blueprint call on BeginPlay (BP_GameMode or BP_PlayerCharacter, your judgement on the natural home), Get Subsystem with LTGoreDecalSubsystem as the class, then SetDecalMaterials with two materials. The impact material reuses Content/LastTrain/Materials/M_LT_ZombieWound.uasset via a material instance (not the parent). Before trusting it, verify its material domain is actually MD_DeferredDecal and that unreal.MaterialEditingLibrary.get_statistics(mat).num_pixel_shader_instructions is nonzero after a forced recompile, known-issues.md 2.7 explains why this project checks that specifically. The four T_ZombieWound_* textures it needs are not committed (no git-lfs in this environment for *.png), regenerate them with tools/zombie-surfaces/generate.py --kind wound per the command in phase-f6a-zombie-surface-masks.md section 1.1. Leave DeathPoolDecalMaterial null for now, a pool-shaped texture does not exist yet and the subsystem safely skips the pool when it is null; do not try to author or generate one in this pass, that is out of scope here.

2. Tune MaxActiveDecals. This is a measured performance number, not a feel number: docs/tasks/phase-h2-gore-system.md section 2 explains exactly why (deferred decals are a GPU fill rate cost, and this project's frame gate is already GPU bound per the S6 reading in handover.md). Measure GPU frame time with the decal pool empty versus full using the S6 method (packaged Development build, CSV profiler, forced horde), not a PIE eyeball. Start from the default of 48 and only change it if the measurement justifies it, adjusting DecalFadeScreenSize or DecalLifetimeSeconds first if the pool costs real time, the cap only as a last resort. Record whatever you actually measure in the handover.md ledger, in the same style as the existing S-numbered rows.

3. Confirm the restrained read in PIE. Play a full round in L_CanaryWharf_Greybox and check it against art-direction.md section 6, not against how visible the feature is: one small mark per body hit, a headshot reads as a bigger moment (two marks, not a spray), the platform reads as "a fight happened here" after several rounds rather than a slaughterhouse, nothing floats or stamps across a doorway or sign, boarding and travelling to the next station leaves no decals behind (confirm this once, the subsystem is world scoped so it should be automatic).

Accept: SetDecalMaterials is called once on BeginPlay and GetActiveDecalCount() climbs from zero during a round and caps at MaxActiveDecals. The impact material reports nonzero pixel shader instructions after a forced recompile. A GPU frame time reading with the pool full is recorded in handover.md, compared against the S6 baseline. Map Check reports 0 errors and 0 warnings, ignoring the stock lighting rebuild message. All five CI gates pass (tools/ci/, run them locally before calling this done).

When done, add a row to docs/tasks/handover.md documenting what was built, the material and texture provenance, the measured GPU cost, and the final MaxActiveDecals value with your reasoning, same style as the existing rows. If anything in this task turns out to need a Source/ change that was not anticipated, stop and say so rather than guessing at C++, this task is scoped as editor-only work against an already compiled contract.
