# Remote brief: subtitle system

This file is the handoff prompt for a remote Claude session working this
branch, verbatim. Follow it as the task brief.

---

Repo: london-underground-the-last-train, branch off main. This is LAST TRAIN, a first person zombie survival game on a fictionalised London Underground line, built in Unreal Engine 5.8. Read CLAUDE.md at the repo root first, in full, for every project convention: British spelling everywhere including comments and docs, never use em or en dashes anywhere (source, docs, this branch's own commit messages), TObjectPtr not raw UObject* in containers, LT_LOG not UE_LOG, no TODO/FIXME/HACK markers, clang-format 20 with tab indent, headers begin with #pragma once and end their include list with X.generated.h. You have no access to the Unreal Editor and cannot compile or run PIE: write C++ against the existing module conventions, do not attempt to build or test it in-engine, and say so plainly in your summary rather than claiming it compiles.

Context, so this branch does not collide with sibling work: several other remote branches exist alongside this one. The most relevant is docs/tasks/remote-brief-g1-audio.md (on branch claude/phase-g1-audio-prompt), which adds USoundBase property hooks and PlaySound call sites across ULTWeaponData, ULTWeaponComponent, ALTZombieCharacter/ULTZombieTypeData, ALTTrain, ALTRoundManager and ALTInteractionComponent. Read that brief in full now, in this repo or from that branch if it exists, since this branch's subtitle triggers should sit at the same call sites that branch's sounds play from, so a later merge of both onto main lines them up rather than needing a third pass to reconcile them. claude/save-settings-prompt adds a settings save system; a subtitle on/off toggle belongs in that system's settings block, read that brief too and design this branch's toggle to slot into it (a single bool, e.g. bSubtitlesEnabled) rather than inventing a separate settings mechanism.

Task: subtitles were explicitly decided as wanted (2026-09 owner decision, no dedicated spec existed before this branch). Read docs/tasks/phase-g-audio-hud-balance.md's G1 section and docs/reference/reference-frame-notes.md section 3 (the HUD table) for the existing audio and HUD design language, so subtitle presentation matches the restrained HUD philosophy already established ("Explicitly NOT the reference frame's HUD furniture", per phase-g2-hud.md) rather than introducing a busy caption box.

Scope:

1. A small data-driven subtitle line definition. Add Source/LastTrain/Public/Audio/LTSubtitleLine.h (new Audio/ folder, matching the module's existing category-folder convention) as either a lightweight USTRUCT (FLTSubtitleLine: FText DisplayText, float Duration, optionally an FGameplayTag or FName Category for future filtering) or a small UDataAsset per line if that matches this project's existing data asset pattern better (ULTZombieTypeData and ULTWeaponData are both UPrimaryDataAsset, one file per instance - decide whether subtitle lines are numerous and varied enough to warrant that same per-instance-asset treatment, or few and simple enough that a USTRUCT plus a UDataTable row is more appropriate, and say which you chose and why).
2. A subtitle display subsystem or component, Source/LastTrain/Public/Audio/LTSubtitleComponent.h (or a UGameInstanceSubsystem if that is more consistent with the sibling gore decal branch's choice there - read docs/tasks/remote-brief-blood-decals.md's reasoning on UWorldSubsystem versus UGameInstanceSubsystem if that branch exists yet, and make a consistent choice across both rather than picking differently for no reason), exposing a UFUNCTION(BlueprintCallable) ShowSubtitle(const FLTSubtitleLine& Line) or ShowSubtitle(FText Text, float Duration), a UPROPERTY(BlueprintAssignable) delegate broadcasting when a subtitle should appear and clear (FOnSubtitleChanged or similar, carrying the current text, empty text meaning cleared, matching this project's existing delegate-driven HUD pattern rather than a widget polling every tick), and a UPROPERTY(EditAnywhere, BlueprintReadOnly) bSubtitlesEnabled bool that, when false, makes ShowSubtitle a no-op (this is the field the sibling save-settings branch's settings block should end up driving; if that branch has already landed and defines its own settings struct by the time you work this one, read it and wire this bool to read from there instead of duplicating a second settings flag - state clearly in your report which you did).
3. Call sites: do not invent new triggers, hang subtitle calls off the same points the G1 audio branch plays sound from. Concretely, once you have read that brief: zombie vocal cues (idle/aggro/attack/death, per ULTZombieTypeData if that branch put sounds there), train presentation events (arrival, doors, departure, matching ALTTrain's existing nine presentation hooks), round stingers on ALTRoundManager, and the interaction stinger on ALTInteractionComponent. Each subtitle line is plain descriptive text, not a transcription of a real recording (per CLAUDE.md's legal rule against transcribed announcements - a subtitle for an original in-fiction announcement is fine, a subtitle is not itself audio and does not reproduce anything, but keep the wording original and in the project's own voice, not lifted from any real transit announcement phrasing). If the G1 audio branch has not landed yet when you do this work, add the subsystem and its API complete and correct, but only wire the call sites you can find clear existing non-audio triggers for today (OnZombieDied, the interaction delegate, ALTTrain's existing hooks), and leave a clear note in your final report listing which call sites are pending that branch landing, do not block this whole branch on a sibling that may not exist yet.
4. Write a short new task spec, docs/tasks/phase-g6-subtitles.md, telling CC-in-Unreal what remains: a subtitle text widget (single line, bottom of screen, in the project's existing HUD typeface per phase-g2-hud.md once that lands, no background box or with a minimal scrim only, matching the restrained HUD philosophy), binding it to the new OnSubtitleChanged delegate, writing the actual subtitle line content once real sound assets are chosen in phase-g1-audio.md (this branch does not author final subtitle copy for every sound, it wires the system and a small number of example lines only, say explicitly in the spec that full subtitle copywriting is a follow-up), and exposing the bSubtitlesEnabled toggle in whatever settings panel the save-settings branch's own follow-up spec builds (cross reference it by name).

Rules: British spelling, no dashes, TObjectPtr not raw pointers, LT_LOG not UE_LOG, no TODO markers, GENERATED_BODY present, headers end their includes with X.generated.h. Original subtitle wording only, no transcribed real announcement phrasing. Keep commits small: the subtitle line type as one commit, the subsystem/component as a second, call site wiring as a third, the spec doc as a final commit.

When done: do not merge or push anything, just leave the branch in a clean, reviewable state. Report back the exact new API (type names, function signatures, delegate shape), which data representation you chose for subtitle lines and why, which call sites you actually wired versus left pending the audio branch, and explicitly flag anything you were not confident about rather than guessing silently.
